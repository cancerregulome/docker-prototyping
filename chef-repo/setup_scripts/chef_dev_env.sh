#!/bin/bash

# The chef user on chef cluster nodes
#export chef_user=chef

# The chef server fqdn
#export chef_server=chef-server-centos.com

# The path to the location on the chef server where the .pem files are
#export pem_path=/home/chef

# The admin client name
#export admin_client=admin

# The validation client name
#export validator=isb-validator

# The location of the chef repo on the workstation
#export chef_repo=/home/abby/docker-prototyping/chef-repo

