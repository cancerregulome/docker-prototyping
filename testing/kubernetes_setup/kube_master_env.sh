#!/bin/bash
# The chef server hostname
export chef_server=chef-server-centos
# The chef user
export user=chef
# The admin users's real name
export fullname="Abby Hahn"
# The admin user's email
export email=ahahn@systemsbiology.net
# The admin user's password
export pass=p@s5w0rD
# The admin users's organization (short version)
export orgshort=isb
# The admin users's organization (full version)
export orglong="Institute for Systems Biology"
# The location of the VM shared folder on the chef server
export vbox_share=/media/sf_vm_setup_scripts
